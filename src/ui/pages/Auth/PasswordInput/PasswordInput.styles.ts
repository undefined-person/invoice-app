import styled from 'styled-components'

export const PasswordInputWrapper = styled.div`
  position: relative;

  svg {
    position: absolute;
    right: 20px;
    top: 20px;
    cursor: pointer;
  }
`
